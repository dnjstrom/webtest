web:
  image: node:5.5-slim
  container_name: web
  ports:
    - "80:3000"
    - "9486:9486" # autoreload
  links:
    - db:database
  volumes:
    - .:/docker
  working_dir: /docker
  command: npm start
db:
  image: mongo:3.2
  container_name: db
