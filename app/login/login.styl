.login-box {
  margin: 5rem auto;
  max-width: 25rem;
  padding: 2rem;
  overflow: auto;
  background: #fafafa;

  input {
    display: block;
    width: 100%;
  }

  button {
    float: right;
  }
}
